.gradle/
build/
.idea/
.kotlin/
*.iml
local.properties
deploy/.env
# Written by the conformance-selftest CI job (and by running it by hand the same way).
.spool-data/
.spool.pid
spool-daemon.log
conformance.tap
build.env
metadata.json
digest
trivy-*.json

# Local load-test scratch: compiled LoadGen classes, per-run logs and results, and the throwaway
# proxy cert/token a run mints. Generated output only — no source lives here.
bench/
